function isValidSubsequence(array, sequence) {
    let j = 0;
      for(let i = 0; i < array.length; i++) {
          if(array[i] === sequence.length) {
          } 
          return true;
      }
  
      
  }
  
  // Do not edit the line below.
  exports.isValidSubsequence = isValidSubsequence;
  